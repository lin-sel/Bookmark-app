import { SortBookmarkByCategoryPipe } from './sort-bookmark-by-category.pipe';

describe('SortBookmarkByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBookmarkByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
